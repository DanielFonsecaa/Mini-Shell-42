Tips to work on

usar strcmp(ja temos implementado);
usar um strjoin_free em que mandamos um int para determinar de libermos str1 ou str2 ou ambas str;
implementar um fd_printf em que mandamos o fd;

ao exandir as variaveis de ambientes com aspas nao divide em tokens, e sem aspas nao divide em tokens
ex: export VAR="hello        world"
echo $VAR  -------- hello world
echo "$VAR" --------- hello      world
mkdir $VAR -------- cria 2 pastas uma hello outra world
mkdir "$VAR" -------- cria so uma 'hello       world'


se apagarem o diretorio, o que acontece o prompt.

echo \n prints n
echo \\n prints \n
fix \ -- literal meaning

 echo | pwd | cd | export | unset | exit | env

 cat | cat | cat | ls

 mkdir test
 mkdir test2
 cd test/test2
 rm -rf ../../test

export var="s -"
l$var"l"
cat > $var -- not
cat > "$var" -- yes
cat > "l"$var -- not
cat > l"$var" -- yes
cat > "l"$var2 -- yes

shell level fix    1000 >= reset to 1 or if it doesnt exist create with 1

cat e depois mandar CTRL + \