Tips to work on

usar strcmp(ja temos implementado);
usar um strjoin_free em que mandamos um int para determinar de libermos str1 ou str2 ou ambas str;
implementar um fd_printf em que mandamos o fd;


se apagarem o diretorio, o que acontece o prompt.

 echo | pwd | cd | export | unset | exit | env

 cat | cat | cat | ls

 mkdir test
 mkdir test2
 cd test/test2
 rm -rf ../../test

export var="s -"
l$var"l"
cat > $var -- not
cat > "$var" -- yes
cat > "l"$var -- not
cat > l"$var" -- yes
cat > "l"$var2 -- yes

shell level fix    1000 >= reset to 1 or if it doesnt exist create with 1

cat e depois mandar CTRL + \


LEAKS!!!!!
cat << eof > file | cat file - FIXED

ls " -la" | "ls -la" | cat - FIXED

../..

acho que sao da mesma fonte de erro, corrigindo um corrige o outro
"ls -la | echo a" "|" | ls - FIXED
"echo a > test.txt" - FIXED
"ls | cat | echo a" | "ls" " -la" | cat - FIXED

../..

""|"""ls""-la""|"|"ls"

echo "$"a -> temos de printar o dollar talvez

echo "" > file

random command tipo assdff - FIXED

echo $variavelnaoexistente -> muitooooos leaks e core dump -> fixed for now
qualquer cena com variaveis nao existentes crasha -> fixed for now
